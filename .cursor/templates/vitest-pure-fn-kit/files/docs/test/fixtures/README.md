# 测试夹具目录

存放 **人工浏览器勾验**、接口联调复现用的静态夹具（非 Vitest 自动化用例）。

## 用途

- 选文件夹扫描：最小目录树 + 白名单命中样本
- UI 全流程：按 `docs/test/` 测试文档逐步勾验

## 约定

- 夹具宜 **小**、可提交；勿复制整仓
- 变更夹具时同步更新 `docs/test/` 对应用例说明

## 本仓示例（WorkBench）

- `fabric-e2e/`：工作台选夹 E2E 人工夹具（若已从模板启用后自行添加）

新仓启用模板时此目录仅含本 README；按业务补充子目录。
