---
description: Use when finishing code changes — run lint/test/build before claiming done.
globs: src/**/*.{ts,tsx,js,jsx,mts,cts}
alwaysApply: false
---

# 改完代码先验证

改动了 `src` 下业务代码后、向用户宣称「完成 / 已修好 / 可通过」之前：

1. 执行 `yarn lint`（至少覆盖本次改动相关问题；失败先修再声称完成）
2. 若改动涉及 **纯函数**（见 Skill `vitest-pure-fn`）或对应 `__tests__`，执行 `yarn test`（或 `yarn vitest run` 指向本次测试文件）
3. 若改动涉及类型、接口、`tsconfig`、构建入口或依赖，再执行 `yarn build`
4. 用命令输出作为依据；未跑命令不得声称通过

**不要：** 未经用户明确要求，全仓执行 `yarn lint:fix` 或 `yarn format`。

格式化靠 IDE / Prettier 即可；本规则管 **lint、单测与类型构建门禁**。
